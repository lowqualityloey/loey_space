{
  "version": "v1",
  "hooks": [
    {
      "name": "Inbox Status on Session Start",
      "trigger": "SessionStart",
      "description": "Reports inbox item count, age of the oldest capture, and how many lines are tagged and ready to sweep. Runs once per session instead of nagging on every message.",
      "action": {
        "type": "command",
        "command": "pwsh -NoProfile -ExecutionPolicy Bypass -File \"06-Resources/scripts/inbox-status.ps1\"",
        "timeout": 15
      }
    }
  ]
}