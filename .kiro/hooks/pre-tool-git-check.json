{
  "version": "v1",
  "hooks": [
    {
      "name": "Git Tool Secrets Validation",
      "trigger": "PreToolUse",
      "description": "Validate .env has all required keys",
      "matcher": "git.*|Git.*|commit|push",
      "action": {
        "type": "agent",
        "prompt": "Check for secrets in files being committed. If any file contains patterns like API_KEY=, TOKEN=, PASSWORD=, SECRET=, or actual keys (length > 20), block the operation and warn user."
      }
    }
  ]
}