{
  "version": "v1",
  "hooks": [{
    "name": "Git Commit Secrets Guard",
    "trigger": "PreToolUse",
    "matcher": "execute_pwsh",
    "action": {
      "type": "agent",
      "prompt": "If this command involves git commit or git push, verify no .env, .secrets/, or files containing API keys are being staged. If secrets are detected, block the operation."
    }
  }]
}
