{
  "version": "v1",
  "hooks": [{
    "name": "Git Commit Message Generator",
    "trigger": "PreToolUse",
    "matcher": "git.*commit",
    "action": { "type": "command", "command": "node -e \"const fs = require('fs'); const commitFile = '.git/COMMIT_EDITMSG'; const messages = ['feat: Add new feature', 'fix: Correct bug', 'docs: Update documentation', 'refactor: Improve code structure', 'chore: Update dependencies', 'test: Add test cases']; const currentFile = process.env.KIRO_CURRENT_FILE || ''; const msg = currentFile ? 'Update: ' + currentFile : messages[Math.floor(Math.random() * messages.length)]; console.log('Suggested commit: ' + msg); process.exit(0);\""
  }]
}
